# Strategic Planning System - Production Environment Variables
# Copy this file to .env.production and fill in the actual values

# Application URLs
NEXT_PUBLIC_APP_URL=https://strategic-plan.yourdomain.com
NEXT_PUBLIC_SUPABASE_URL=https://api.strategic-plan.yourdomain.com

# Database Configuration
POSTGRES_PASSWORD=password123

# JWT and API Keys (Generate secure random keys)
JWT_SECRET=your_jwt_secret_256_bit_key_here
ANON_KEY=your_anon_key_here
SERVICE_ROLE_KEY=your_service_role_key_here
REALTIME_SECRET_KEY_BASE=your_realtime_secret_key_base_here

# Email Configuration (SMTP)
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=password123

# Optional: External API Keys (if needed)
# PPLX_API_KEY=your_perplexity_api_key
# CLAUDE_API_KEY=your_claude_api_key

# Security Settings
NODE_ENV=production

# Instructions:
# 1. For development, all passwords are set to 'password123' by default
# 2. For production, generate secure passwords and keys using: openssl rand -base64 32
# 3. For JWT_SECRET: openssl rand -base64 64
# 4. Replace all placeholder values with actual secure credentials
# 5. Never commit this file to version control
# 6. Set proper file permissions: chmod 600 .env.production
